import { GainerDirective } from './gainer.directive';

describe('GainerDirective', () => {
  it('should create an instance', () => {
    const directive = new GainerDirective();
    expect(directive).toBeTruthy();
  });
});
