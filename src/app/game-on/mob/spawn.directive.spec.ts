import { SpawnDirective } from './spawn.directive';

describe('SpawnDirective', () => {
  it('should create an instance', () => {
    const directive = new SpawnDirective();
    expect(directive).toBeTruthy();
  });
});
